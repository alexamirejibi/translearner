
"""
class learn_model(object):
    def __init__(self, args, gamma):
        self.args = args
        self.gamma = gamma
        #self.env = gym.make(ENV_NAME)

        #self.dims = (SCREEN_WIDTH, SCREEN_HEIGHT)

        # self._screen = None
        # self.reward = 0
        # self.terminal = True

        # self.reset()
        # if self.args.lang_coeff > 0:
        #     self.setup_language_network()
        #     self.gamma = gamma
    
    def get_nl_reward(self, trajectory):
"""